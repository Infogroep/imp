#!/bin/bash

if [[ "$1" == "--here" ]]; then
	file="$(util/store reserve-file)"
	uri="$2"

	{
		wget -O "$file" "$uri"
		util/player reevaluate "$file"
	} &

	shift 2
	util/player enqueue "$file" "$@"
else
	"$0" "--here" "$@" & disown
fi
